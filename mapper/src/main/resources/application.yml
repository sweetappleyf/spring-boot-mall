spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/bishemall?characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
      username: root
      password: yf

mybatis:
  mapper-locations: classpath:mappers/*Mapper.xml
  #和beans关联
  type-aliases-package: com.yfmal.entity
logging:
  level:
    io.swagger.models.parameters.AbstractSerializableParameter: error
